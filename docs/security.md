# Security notes

## Secrets

Do not commit API keys. Use environment variables or a local config file with env placeholders.

## Fetch safety

The fetch tool includes private and internal address protections. Treat any bypass as a security issue.

## Paid providers

Hosted provider calls are optional and controlled by explicit configuration. Use budget guardrails when using paid providers.
